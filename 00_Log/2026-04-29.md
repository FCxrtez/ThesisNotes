# Tipos de branch predictors en gem5

## Local predictor (LocalBP)

The Design: It tracks the history of each branch individually based on its Program Counter (PC).
The Drawback: It completely ignores correlation between different branches.
How to force misses: Write a workload with "correlated branches"—where the outcome of one if statement guarantees the outcome of the next.
