# Sobre custom instructions en cva6

Parece que no hay posibilidades de usar la extension de instrucciones custom en cva6. Esto nos facilitaria ciertos detalles como que el opcode ya estaria definido. Es decir que vamos a tener que:

- a

La instruccion a agregar entraria cerca de la linea 470 en `decode.sv`, ya uq ees la seccion de operaciones de punto flotante.

no muy relacionado a lo visto hoy pero el proceso de construccion de la configuracion para el proceso tiene varias etapas. Se parte de una configuracion especifica de lo que queremos que incorpore nuestro procesador que sale de `cva6_config_pkg::cva6_cfg`. Esta estructura es pasada a `build_config_pkg::build_config`, que la toma y creo que que haces es definir algunos campos mas utilizando algunos de los campos que ya teniamos definidos.

Notese que build_config es una funcion que toma `config_pkg::cva6_user_cfg_t` y devuelve `config_pkg::cva6_cfg_t`. Ambas definiciones de estructuras pueden encontrarse en `config_pkg.sv`
