# Correccion en como se tratan los numeros subnormales

Me di cuenta que estamos tratando los inputs subnormales mal al calcular la diferencia de exponentes. Ya que los numeros subnormales con `exponente = 0` tienen el mismo peso que los que tienen `exponente = 1`, solo que los subnormales son de la forma `0.xxxx_xxxx_xx` en la alineacion de exponentes tenemos que tratar ambos exponentes de la misma manera, es decir que cuando calculamos los shifteos para cada mantisa los shifteos deben ser los mismos si el exponente es 1 o 0. Esto es simplemente un mux. Para no terminar introduciendo otro comparador a cero lo que podemos hacer es usar la señal de numero subnormal que sale del clasificador y que esto entre en un mux. **Si el numero es subnormal el exponente que pasa al modulo de `exp_align` es 1, sino es el exponenete original.**

- cambio aplicado en modelo, simulacion en sv falla donde debe (señales shifteo, implicit mantissas aligned, etc)
- cambio aplicado en sv, simulacion pasa correctamente todos los assertions. Ademas en el modelo cambiamos la linea: `monitor.record("exponentes", f"{pp_bits_bus(exponentes, 5)}")` por `monitor.record("exponentes", f"{pp_bits_bus(exponentes_exp_align, 5)}")` para evitar el error de assertion en la simulacion y no generar nuevos cables al vicio.

TODO:

- Escribir empaquetado final del resultado
- Implementar logica de casos especiales (por ejemplo `+Inf + -Inf = QNaN`). Este paso lo podriamos saltea, y realizar casos de testeo donde no sea necesaria esta logica.
- Implementar automatizacion de flujo tomando ahora el golden model de numpy, modelo de python y simulacion de system verilog. Aqui tambien podriamos solo comparar resultados de golden model con el modelo de python para darle mas "jerarquia" al modelo.

---------------

Escribi el empaquetado final, parece andar bien los casos que probe a mano. Lo unico que me llamo la atencion es que usando numpy, el tipo `numpy.float16` cuando casteamos numeros como `65504.0` que se representa como `signo = 0, exp = 1_1110, mantisa = 11_1111_1111` el resultado que muestra la terminal es otro, es decir que en numpy ese numero esta representado por otro valor dentro de todos los que representa la palabra binaria `0111_1011_1111_1111`. En el sumador si pasamos `65504 + 0 + 0 + 0` obtenemos `65504` mientras que en numpy:

```python3
>>> import numpy as np
>>> np.float16(65504)
65500.0
```

Por que lo que me estoy cuestionando como escribir los random test. Ya en el caso de `65504` vamos a tener un error. Tambien se me ocurre que los test pueden ser realizados con una mayor precision, para evitar estos redondeos ya que

```python3
>>> np.float32(65504)
65504.0
```
