# Fin implementacion LZA en sv

Termine al implementacion y agregue la señales a chequear en el tb. En un futuro habra que dividir el contenido de `tb_pkg` ya que contiene cosas que no son sintetizables, como las clases que uso en el tb, y al mismo tiempo contiene estructuras que se necesitan para describir el hw. En un futuro esto va a generar problemas, se va a descartar el paquete entero por las clases y no se va a poder sintetizar el hw.

Queda un poco mas de logica a implementar en SV. Luego hay que seguir por:

- Implementar logica de redondeo.
- Implementar golden model con NumPy.
- Implementar suite de testeo y testbench para determinar que mejoras introducen distintos cambios o nuevas implementaciones como: Catastrophic cancellation detention, sticky bit logic.

----------------------

Sv ya esta a la misma altura que modelo de python y pasa los asserts del tb.
