# RoadMap

## 1. Definicion de instruccion

Definir tipo, campos, opcode, nombre de instruccion. Podemos encontrar en `decoder.sv` que tipo de instrucciones son las instrucciones vectoriales de la FPU en ~470. Ademas en `riscv_pkg.sv` podemos encontrar las definiciones de las estructuras de todas las instrucciones dentro de `riscv::instruction_t`. Notese ademas que no podemos utilizar la extension de instrucciones custom ya que cva6 no las implementa.

## 2.Pasos con orden intercambiable

### 2.1 Cambios en `decoder.sv`

No tengo claro que detalles debemos tener el cuenta o puedan a llegar a limitarnos, pero hay que preparar la etapa decode con lo que necesite el `fpu_wrap.sv`. Del decoder sale directamente la senial de tipo `scoreboard_entry_t`. Podemos encontrar en `id_stage.sv` la estructura `issue_struct_t` que componemos a partir de seniales output de `decoder.sv` para comunicarnos con el issue stage. Luego, en issue stage esa senial va hacia `scoreboard.sv` y `issue_read_operands.sv`. No creo que haga falta realizar cambios en estos modulos, solo en `decoder.sv` y `fpu_wrap.sv`.

seniales de control?

Ademas, agregar la instruccion en `fpu_wrap.sv`.~175.

#### Verificacion ID

Controlar que dado una instruccion se setteen correctamente seniales de control y demas? Muy dificil de llevar a cabo?

### 2.2 Cambios arquitecturales en FPU

Problemas de overflow? Usar un registro de 32 bits para reducir y transformar a 16 bit antes de devolver? Es necesario? Si no hacemos esto es probable que la reduccion termine constantemente en +-Infinity? Latencia de operacion?

Sobre overflow/underflow como mido la diferencia que generan? 
**Como interactua con el stall, flush, fowarding?**. Ver como interactuan los demas modulos ya implementados de FPU para determinar?

#### Verificacion EX

simil verificacion ID.

### 2.2.1 cambios en `fpnew_pkg.sv`

Agregar la nueva instruccion a `operation_e`.

### 2.2.2 cambios en `fpnew_fma.sv`

No me parece que haga falta cambio en `fpnew_opgroup_fmt_slice.sv` ni `fpnew_opgroup_block.sv`, no me parece coherente por la logica que manejan estos modulos. Podria modificar directamente `fpnew_fma.sv` y ese cambio se aplicaria para todos los tipos.

Arrancar por agregar instruccion en bloque `op_select`, linea ~180. Aqui se define `operand_a,b,c` e `info_a,b,c`. 

**Chequear que pasa con las slices_multi_fmt, afectan cambios?**. Seguramente el codigo este definido en base a parametros. Mas facil 'hardcodear' para solo FP16, pero mas prolijo si escribo codigo parametrizable (seguramnete mas problemas a resolver). Dentro de archivo puedo instanciar un submodulo que cotenga todo arbol de sumadores. Como solucionar coneccion entre salida de arbol y camino comun que no usa el arbol de sumadores? **Arbitrer**, **estoy agregando +1 ciclo de latencia para todas las instrucciones**. habra manera de tomar si hay un producto punto on the fly entonces usar arbitrer, sino no. Simil senia busy a traves de `{| reduccion}` reduccion del pipeline. Ejemplo de senial busy en mismo archivo `assign busy_o = (| {inp_pipe_valid_q, mid_pipe_valid_q, out_pipe_valid_q});`

Puedo hacer alguna optimizacion en cuanto a normalizacion, redondeo? Algun paso a saltear?

- Identificar en archivo seniales de salida para conectar a modulo de arbol sumadores
**Puedo conectar el arbol de sumadores a salida de output pipeline, `fpnew.sv:650`**

- multiplexor entre senial 
`result_o = (<arbitrerOutput> & <ArbolSumadoresBusy>) || (out_pipe_result_q[NUM_OUT_REGS] ^ !<ArbolSumadoresBusy>)`

Podemos usar alguna senial como un valid para el arbol de sumadores?

### 3.Post implementacion arquitectural

Medicion de timing, sintetizable? Manera mas eficiente de controlar que esten bien los cambios? Como verifico que no necesito mas cambios en etapas de commit o wb?

### 4.Benchmarks

Programa estilo

```c
#DEFINE N 1<<31

int main(){
    fp16 A[N], B[N], C[N];
    for( unsigned int i=0; i < N; i=i+4){
        C[i] = dprod(A,B,i);
    }

    return 0;
}
```

Como forzar vectorizacion con compilador riscv?

### 5.Final

- Nuevos diagramas (pre/post cambio).
- Analisis performance, IPC, ...
