# Definiendo addmul_fmt_num_slices_t

Integre el campo a la estructura `fpu_implementation_t` en `fpnew_pkg.sv`. Estoy revisando de que manera inicializar los valores, de la documentacion saque:
**Subclause 5.10 discusses assigning initial values of a structure.**

De todas maneras, para que cva6 lo use hay que modificar el archivo en `core/fpu_wrap.sv` pues ahi definimos las caracteristicas de la implementacion de nuestro procesador. Algunos valores vienen dados de los parametros del package de cva6, otros hardcodeados.

En 10.9 podemos encontar Assingment patterns

En 5.10 podemos encontrar struct literals
