# Sobre modificaciones

## cambios en fpnew

### `fpnew_pkg.sv`

- .110 = se modifico el parametro `NUM_OPGROUPS` a 5
- .114 = se agrego el opgroup REDUC al enum `opgroup_e` y se agrego un bit al enum para representar el nuevo
- .124 = se agrego operacion `FREDSUM`
- .117 = se modifico el parametro `OP_BITS` a 5 porque con 4 no podiamos soportar 17 operaciones distintas.
- .384 = definimos la funcion auxiliar `get_opgroup` para `FREDSUM`
- .396 = defijnimos la funcion auxiliar `num_operands` para **`REDUCTION`**

### `fpnew_opgroup_fmt_slice`

- .98 = agregamos la condicion `(EnableVectors && OpGroup != fpnew_pkg::REDUCTION)` para que no se generen mas de 1 lane en el caso de `OpGroup == REDUCTION`. Para el resto de los valores en los buses se settea como dice el else en ~.260
- para que sirve senial `is_boxed_i`
  
## cambios en cva6

### `fpu_wrap.sv`

- .90 = agregamos la latencia de modulo reduccion a `FPU_IMPLEMENTATION`
- Especial atencion a el parametro `FPU_IMPLEMENTATION` en `fpu_wrap.sv`, hay que ver como definimos nuevo modulo para ver que parametros pasar.
- .397 = agregamos la "traduccion" de la operacion de `ariane_pkg` a `fpnew_pkg`. Alguna cosa mas que sea necesaria settear?

### `decoder.sv`

- .763 = agregamos nueva caso para decodificar `VFREDSUM`. Ademas seteamos bits para chekear rounding mode y replication. Replication determinada por bit de instruccion.

### `ariane_pkg`

- .54 = se agrego parametro `LAT_REDUCTION` que luego sera utilizado para definir cuantos pipelines tendra la unidad, ahora seteado a 0, se analizara el timing una vez implementada la unidad.
- .434 = se agrego `VFREDSUM` a la estructura `fu_op`, enumerado para definir instrucciones dentro de la arquitectura.
- Realice cambios en funciones `is_rd_fpr, is_rs1_fpr, is_rs2_fpr` que se usan para las instruction tracers en `ariane_pkg.sv`, pero no se verdaderamente si el cambio fue correcto ya qu eno entiendo para que se usan esas funciones.

### `fpnew_top.sv`

### a resolver

Arbitrer funciona de misma manera con 5 opgroups en vez de 4? Parametros deberian estar bien setteados

Chequear donde se settea `dst_fmt_i` senial qeu llega a fpnew? En Issue Stage, en modulo `issue_read_operands.sv`

Chequear que FP16 este definido como `fpnew_pkg::PARALLEL` en estructura `FmtUnitTypes` ya que sino va a intentar generar una unidad mergeada y no esta implementada.

Que es NaN-box? Creo que especifico de RISC-V -- manera de devolver los numeros que no ocupan la totalidad del bus de datos, llenamos el resto del bus con '1

### a resolver en `fpnew_opgroup_fmt_slice`

- Forzar a que se genere un solo modulo de `fpnew_reduction.sv`: generate if? **OK**
- Cuantos operados? Principalmente 2 para vector y mascara, podriamos implementar hasta 3 operandos para reduccion de mayor tamanio o producto-punto. **OK**
- Si vamos a pasar una mascara en rs2 y el registro en rs1, como hago para que la mascara pase hasta la unidad de computo y no vaya a buscar un valor a los registros?
- Notar que los parametros `TrueSIMDClass` y `EnableSIMDClass` son parametros que se usan para instrucciones de clasificacion y se settean por el default `=0` ya que no se utilizan en `fpu_wrap` **REVISAR**
- Determinar como se seleccionan los resultados a partir de la linea `assign result_o = result_is_class ? slice_class_result : slice_regular_result;` y `assign slice_class_result = result_is_vector ? slice_vec_class_result : lane_class_mask[0];`. Relacionado a `aux_o`(Relacionado a result_is_vector que despues ) y `extension_bit_o`(Relacionado a NaN-box) **REVISAR**
