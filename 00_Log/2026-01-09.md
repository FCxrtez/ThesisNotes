# Tareas 9 enero

como escribir a una direccion de memoria fisica. problema a nivel OS

modelo de memoria.

que registros de estado tiene CSR.

Encontrar informacion
como setear vectores en FPU? por instruccion?

!!!!
Diagrama - hoja de ruta para realizar implementacion
Idea general de los cambios - diagramas en lo posible de la idea, arbol de sumadores, etc..
!!!!

Invesitgacion issues de repo para ver que hablan de las instrucciones vectoriales, registros vectorizados.

preguntar en foro sobre instruccion

Descartar la vectorizada de la reduccion
Idea es usar instrucciones de FP como vectores

instruccion reduccion de vector
Crear codigo que muestre como implementaria la misma semantica usando instrucciones existentes.

trabajar bottom-up (fpnew a decoder) o top-bottom (decoder a fpnew)

implementar sin registros al medio de la ligica y ver timing
Forzar registros a salida de fpnew_fma e implementar logica antes de registro.

implementar a nivel fpnew_opgroup_fmt_slice.sv

codigo/latencia con configuracion de FF en fpnew_fma.sv

definir configuracion final de pipelines dentro de `fpnew_fma.sv` para iniciar desarrollo.

sobre latencias, implementar primero hw de reduccion sin pipeline y analizar timing, intuimos que deberia cerrar timing sin necesidad de agregar registros.
