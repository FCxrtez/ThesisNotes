# Final implementacion LZA

Generacion de strings confirmado que son correctos para casos probados. Falta chequear que LZD funcione correctamente, que los arboles reduzcan correctamente Gp y Gn. Para testear podemos usar assertions como que el output del LZA y el shifteo real tenga una cota de error de 1, mas ejemplos pedir a IA. Como voy a testear todo ese circuito? Implementado en sv podemos usar assertions y propiedades, metodos formales, aunque nose si eso salga del scope de la tesis.

Sobre el las señales finales la IA dedujo que el la señal `Y = ... | ((V ^ 1) & (N_p & Z_n1))` es compuesta por el ultimo bit del string Z de el LZA (no del pre-encoding). Lo lei y parece ser correcta la deduccion. Si llegaramos al caso de que no puedo deducir la correctitud podemos quitar la logica del error detection y agregar un chequeo post LZA, alargando el critical path.

Queda por implementar:

- Normalizacion
- Redondeo
- logica de Sticky bits
- Catastrophic cancelation?
- Casos especiales

Porque en codigo de FPNew no hay mas chequeos o assertions? Suele manejarse aparte el testeo y una vez confirmado el correcto funcionamiento 