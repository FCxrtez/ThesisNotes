# fpnew_opgroup_fmt_multi_slices.sv

Creamos un nuevo archivo en el repositorio para mantener prolijidad. Hay que evaluar los distintos casos de generar slices paralelas ya que para cada caso se comporta de distinta manera. Tenemos:

+ Caso normal de formato paralelo
+ Caso formato no activado
+ Caso 

Dentro de `fpnew_opgroup_block.sv` tenemos `logic [NUM_FORMATS-1:0] fmt_in_ready, fmt_out_valid, fmt_out_ready, fmt_busy;`. Dentro del generate for `gen_parallel_slices` vamos a tener que modificar como mandamos la senial de busy ya que queremos no indicar busy en los casos en que permanezca una slice libre. Es decir que debemos hacer algo como
`fmt_busy = slice1_busy & slice2_busy & ...`

Tengo que revisar el comportamiento de la unidad de fpu, y como se utiliza la senial de busy dentro de cva6. Como decide el scoreboard cuando debe hacer issue de instruccion si no utiliza la senial `busy_o`? en `fpu_wrap.sv` se ve que no es utilizada.

Se me ocurrio que para no trabame intentando de seguir la senial de busy y entendiendo la logica de los issues puedo diseniar la parte de la fpu independientemente de eso, lo resuelvo mas adelante.
