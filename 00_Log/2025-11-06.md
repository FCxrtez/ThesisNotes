# nueva idea

Replicar hw no va a funcionar porque unidades pipelineadas toman 1 instruccion nueva cada ciclo. Hacemos 1 issue por ciclo -> 0 utilizacion de segunda unidad.

Chequear si no hay instruccion que multiplique y acumule vectorialmente fp16b

+ Si no existe ver que instrucciones podemos usar (vector multiply y vector accumulate y cuantos clks lleva). Si existen ver cuanto tardaria y comparar codigo con nuestra instruccion
+ Como implementar nueva unidad?
+ Existe esta instruccion en RISC-V
  
## Configuraciones de CVA6 en core/include

Podemos encontrar algunas configuraciones hechas por ThalesGroup. Ninguna incorpora vectores ni FP de 16/16Alt/8 bits, deberia investigar por configuraciones que usen en papers que nos interesen/en internet que setteen la FPU para FP16bits, podemos ver como configuran.

CREO que los archivos que buildean toda la configuracion y procesador son:

+ `build_config_pkg.sv` con la funcion `function automatic config_pkg::cva6_cfg_t build_config(config_pkg::cva6_user_cfg_t CVA6Cfg);`
+ `ariane_pkg.sv`. Deberia invesitgar que es lo que implementa, pero entre todo define los tipos de fu, fu_op, frontend_exception_t y mucho mas
+ y `config_pkg.sv`?
